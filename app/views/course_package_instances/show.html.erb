<p id="notice"><%= notice %></p>

<p>
  <strong>Course:</strong>
  <%= @course_package_instance.course_id %>
</p>

<p>
  <strong>Course package:</strong>
  <%= @course_package_instance.course_package_id %>
</p>

<p>
  <strong>User:</strong>
  <%= @course_package_instance.user_id %>
</p>

<%= link_to 'Edit', edit_course_package_instance_path(@course_package_instance) %> |
<%= link_to 'Back', course_package_instances_path %>
