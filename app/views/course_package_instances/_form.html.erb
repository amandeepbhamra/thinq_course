<%= form_for(@course_package_instance) do |f| %>
  <% if @course_package_instance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_package_instance.errors.count, "error") %> prohibited this course_package_instance from being saved:</h2>

      <ul>
      <% @course_package_instance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %><br>
    <%= f.number_field :course_id %>
  </div>
  <div class="field">
    <%= f.label :course_package_id %><br>
    <%= f.number_field :course_package_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
